@helper Time(DateTime d) {
    @(d.Date != DateTime.Today.Date ? "on " : "")<time datetime="@d.ToUniversalTime()">@(d.Date == DateTime.Today.Date ? "Today" : d.Date == DateTime.Today.AddDays(-1).Date ? "Yesterday" : d.ToLongTimeString())</time>
}